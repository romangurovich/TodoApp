<%= form_for(project) do |f| %>

  <p>
    <%= f.label :name, "Project Name" %>
    <%= f.text_field :name %>
  </p>

  <p>
    Items
    <%= check_box_field project, Item, :title %>
  </p>

  <p>
    <%= f.submit project.persisted? ?
            "Edit that project, friend" : "Make this project" %>
  </p>

<% end %>


<%= undo project %>