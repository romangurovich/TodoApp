<%= content_for(:title, "Users") %>

<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_path(user) %>
    </li>
  <% end %>
</ul>

<ul class="nav nav-pills">
  <li class="active">
    <%= make_new User %>
  </li>
</ul>

<%= back_to Team %>
